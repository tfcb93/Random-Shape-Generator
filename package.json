{
  "name": "random-shape-generator",
  "version": "1.0.0",
  "description": "It creates shapes that \"walks\" through its canvas for sometime.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html",
    "build": "parcel build index.html"
  },
  "keywords": [
    "create coding",
    "random shapes",
    "canvas"
  ],
  "author": "Thiago Borges",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.0.0"
  }
}
